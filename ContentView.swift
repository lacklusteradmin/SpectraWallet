// Compatibility shim for older editor tabs and bookmarks.
//
// The app entry point and real ContentView implementation now live at:
// Views/App/ContentView.swift
//
// This file intentionally stays out of the Xcode target after the project
// reorganization, so keeping only a pointer here avoids duplicate symbols
// while preventing stale root-level references from appearing as deleted.
