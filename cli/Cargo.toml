[package]
name = "spectra-cli"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "spectra"
path = "src/main.rs"

[dependencies]
spectra_core = { path = "../core" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
